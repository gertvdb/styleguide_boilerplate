{# ----------------------------#}
{# ------Styleguide block------#}
{# ----------------------------#}
{% macro styleguide(DATA) %}
	<div class="styleguide__h2 styleguide__title js-scroll-nav__sub-title">Colors</div>
	<div class="styleguide__small">/sass/base/_quarks/_colors.scss</div>

	{#  Primary colors #}
	<div class="styleguide__h3 styleguide__sub-title">Primary colors</div>
	<div class="styleguide__p">
		Always provide different shades of the primary colors. For a more high-level approach on color use.
	</div>

	<ul class="a-inline-list styleguide__colors">
		{% for name, color  in DATA.COLORS.PRIMARY_COLORS %}
			<li style="background-color: {{ color.value }}"><small><strong>${{ name }}</strong><br />{{ color.value }}</small></li>
		{% endfor %}
	</ul>

	{#  Secondary colors #}
	<div class="styleguide__h3 styleguide__sub-title">Secondary colors</div>
	<div class="styleguide__p">
		Always provide different shades of the secondary colors. For a more high-level approach on color use.
	</div>

	<ul class="a-inline-list styleguide__colors">
		{% for name, color  in DATA.COLORS.SECONDARY_COLORS %}
			<li style="background-color: {{ color.value }}"><small><strong>${{ name }}</strong><br />{{ color.value }}</small></li>
		{% endfor %}
	</ul>

	{#  Text colors#}
	<div class="styleguide__h3 styleguide__sub-title">Text colors</div>
	<div class="styleguide__p">
		Use different shades for text to express the hierarchy of information.
		Text, icons, borders and dividers will benefit when their color is defined
		with alpha values, as a result they will work on any background. Text colors
		are derived from primary black/white colors.
	</div>

	<ul class="a-inline-list styleguide__colors">
		{% for name, color  in DATA.COLORS.FONT_COLORS %}
			<li style="background-color: {{ color.value }}"><small><strong>${{ name }}</strong><br />{{ color.value }}</small></li>
		{% endfor %}
	</ul>

	{#  Border colors #}
	<div class="styleguide__h3 styleguide__sub-title">Border colors</div>
	<div class="styleguide__p">
		Keep border colors as subtle as possible.
	</div>
	<ul class="a-inline-list styleguide__colors">
		{% for name, color  in DATA.COLORS.BORDER_COLORS %}
			<li style="background-color: {{ color.value }}"><small><strong>${{ name }}</strong><br />{{ color.value }}</small></li>
		{% endfor %}
	</ul>

	{#  UI COLORS #}
	<div class="styleguide__h3 styleguide__sub-title">UI-kit colors</div>
	<div class="styleguide__p">
		Primary colors for interaction feedback, notifications and states:
		error (red), warning (orange), highlight (yellow), success (green)
		and information (blue). UI-kit colors should always be aligned
		with the tone of voice of the application/brand.
	</div>

	<ul class="a-inline-list styleguide__colors">
		{% for name, color in DATA.COLORS.UI_COLORS %}
			<li style="background-color: {{ color.value }}"><small><strong>${{ name }}</strong><br />{{ color.value }}</small></li>
		{% endfor %}
	</ul>
{% endmacro %}
